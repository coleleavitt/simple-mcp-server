[package]
name = "simple-mcp-server"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.0", features = ["full", "process"] }
serde_json = "1.0.143"
mcp-sdk = { path = "./mcp-sdk" }
async-trait = "0.1.89"
chrono = { version = "0.4.41", features = ["serde"] }